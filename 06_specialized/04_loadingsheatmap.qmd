# do_LoadingsHeatmap() | PCA loadings

```{r}
#| results: "asis"
#| echo: false
source("../_common.R")
```

Loadings heatmaps show which genes contribute most to principal components.

## Basic usage

```{r}
#| eval: false
p <- SCpubr::do_LoadingsHeatmap(sample = sample,
                                dims = 1:5)
p
```
