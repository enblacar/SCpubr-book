# do_StripPlot() | Geyser plots

```{r}
#| results: "asis"
#| echo: false
source("../_common.R")
```

Strip plots (also known as geyser plots) show individual observations with density-based jittering.

## Basic usage

```{r}
#| eval: false
p <- SCpubr::do_StripPlot(sample = sample,
                          features = "CD3D")
p
```
