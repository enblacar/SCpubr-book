# do_CorrelationHeatmap() | Feature correlation matrix

```{r}
#| results: "asis"
#| echo: false
source("../_common.R")
```

Correlation heatmaps show pairwise correlations between features.

## Basic usage

```{r}
#| eval: false
p <- SCpubr::do_CorrelationHeatmap(sample = sample,
                                   features = c("CD3D", "CD8A", "CD4"))
p
```
